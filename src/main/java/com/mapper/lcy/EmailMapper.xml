<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.dao.lcy.EmailDao">
<insert id="EmailAdd" parameterType="Email">
   insert into Mailboxinfo2(persoId,TO_ID2,SUBJECT,CONTENT,SEND_TIME,ATTACHMENT_ID,ATTACHMENT_NAME) values(
#{persoId},#{TO_ID2},#{SUBJECT},#{CONTENT},#{SEND_TIME},#{ATTACHMENT_ID},#{ATTACHMENT_NAME}
   )
</insert>

</mapper>